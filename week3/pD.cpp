#include <bits/stdc++.h>
using namespace std;

int main(){
    int n1, n2, ret, filas;
    cin >> n1 >> n2;
    int columnas[n1];
    memset(columnas, 0, sizeof(columnas));
    for (int i = 0; i<n1; ++i){
        for (int j = 0; j<n2; ++j){
            cin >> arr1[i][j];
        }
    }
    for (int i = 0; i<n1; ++i){
        for (int j = 0; j<n2; ++j){
            if (i == 0)
            ret += arr1[i][j];
        }
    }

}
